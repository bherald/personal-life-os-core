# Media Fixtures

This directory intentionally starts without binary photo fixtures. Face,
metadata, and media tests should use generated images or text fixtures unless a
public-domain sample is added with explicit provenance in
`tests/Fixtures/PROVENANCE.md`.

The first public slice uses `face-regions-sample.xmp` to exercise metadata
parsing/writeback shape without shipping a real person or private image.
