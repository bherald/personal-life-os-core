# Optional transformer, HTR, embedding, and transcription dependencies.
# Plain pip installs PyTorch CPU wheels. For CUDA hosts, install the matching
# PyTorch wheel/index first, then install this file.

-r requirements-media.txt

torch>=2.2,<3
torchvision>=0.17,<1
transformers>=4.40,<5
sentence-transformers>=3,<6
openai-whisper>=20231117
tiktoken>=0.12,<1
sentencepiece>=0.2,<1
